#! /bin/zsh

cleanup ()
{
  kill -s SIGTERM $!
  exit 0
}

trap cleanup SIGINT SIGTERM

while true; do
  {ls **/*py; ls datahog/.installed; } | grep -v ^build | entr -d dshell databacon_datab 'PYTHONPATH=/src/ python /src/tests/test.py && date';
done
